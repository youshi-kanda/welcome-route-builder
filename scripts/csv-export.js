// Moved csv-export.js into scripts/
// Minimal wrapper to re-export functionality for references
if (typeof window !== 'undefined') {
    // original code expected globals; we keep file in scripts/ for static hosting
}
// (Content preserved in root original — this file is a lightweight shim.)

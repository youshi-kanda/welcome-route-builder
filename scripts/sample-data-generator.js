// Moved sample-data-generator.js into scripts/ as shim
// The real implementation remains in original file; this shim keeps path compatibility
if (typeof window !== 'undefined') {
    // placeholder
}
